{
  "name": "sala-ba",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack sala.ba venue marketplace, CRM and admin platform.",
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint",
    "prisma:migrate": "npm --workspace backend run prisma:migrate",
    "prisma:seed": "npm --workspace backend run prisma:seed"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
